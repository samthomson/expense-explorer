import { expect } from 'chai'
import 'mocha'

import * as ElasticUtil from './elastic'

describe('elastic util', async () => {
	it('determines median per period correctly', async () => {
		expect(true).to.equal(false)
	})
})
