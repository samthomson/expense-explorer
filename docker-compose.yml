version: "2"
services:
  server:
    build:
      context: ./server
    command: yarn run watch
    environment:
      - PORT=3100
    ports:
      - "3100:3100"
    volumes:
      - ./server:/server
